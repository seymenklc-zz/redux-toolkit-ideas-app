import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    textfield: {
        marginTop: 20,
        marginBottom: 20,
        display: 'block'
    },
    button: {
        float: 'right',
    },
    paper: {
        padding: 10
    },
});