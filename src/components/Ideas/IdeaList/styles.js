import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    paper: {
        padding: 30
    },
    link: {
        textDecoration: 'none'
    },
    fab: {
        margin: 0,
        top: 'auto',
        right: 20,
        bottom: 20,
        left: 'auto',
        position: 'fixed'
    }
});